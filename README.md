# JOB AC — visual evidence (branch is evidence only, never merged)

`contact-sheet.png` — all 15 served routes after the fix, Chrome headless 1280x800,
cache disabled, construction modal suppressed. Individual page captures alongside it.

Nothing here is published: squidbay.ai's asset boundary is `public/` on `main`.
