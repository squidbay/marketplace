# JOB AE — visual evidence (evidence-only branch, never merged)

- `notice-options.png` — before + the three restyle options, side by side.
- `contact-sheet-notice.png` — all 15 served routes, fresh visitor (notice up).
- `contact-sheet-dismissed.png` — all 15 served routes, dismissed/persisted.
- `audit-data.json` — the raw 30 page-state measurements behind the tables.

Chrome headless 1280x800, cache disabled. Nothing here is published: squidbay.ai's
asset boundary is `public/` on `main`.
